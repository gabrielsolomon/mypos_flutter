import 'package:flutter/material.dart';

class InTransactionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(children: [
      Center(
        child: Text('IN TRANSACTION'),
      ),
    ]);
  }
}
